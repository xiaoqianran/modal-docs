<!-- modal-docs: machine-translated zh-CN from English source -->

#`modal bootstrap`

初始化示例模态应用程序。

**用法**：

```shell
modal bootstrap [OPTIONS] [NAME]
```

**选项**：

* `-o, --output TEXT`：模板存放位置。
* `--force`：如果输出目录已存在，则覆盖该目录。
* `--help`：显示此消息并退出。